version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  apt_packages:
    - graphviz
    - plantuml

sphinx:
  configuration: doc/source/conf.py
  fail_on_warning: true

python:
  install:
    - method: uv
      command: sync
      groups:
        - doc
